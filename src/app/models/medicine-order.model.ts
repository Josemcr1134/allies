export interface MedicineOrder {
  appointment: string,
  medicine: any,
  quantity: number,
  unit_measure: string,
  every: string,
  time_measure: string,
  during: string,
  period_measure: string,
  via_measure: string,
  observations: string,
  recommendations: string,
}
